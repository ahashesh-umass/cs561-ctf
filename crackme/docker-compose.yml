version: '1.0'
services:
  ahashesh-ctf-crackme:
    container_name: ahashesh-ctf-crackme
    build:
      dockerfile: Dockerfile
      context: ./
    ports:
    - '3000:80'
    networks:
      nw0:

networks:
  nw0:
    external: true